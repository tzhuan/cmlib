
#include <stdio.h>

extern float *loadCRW(char *filename, int &imgWidth, int &imgHeight);

float* readCRW(const char *filename, int &width, int &height, int &depth)
{
	//depth = 1;
	//return loadCRW(filename, width, height);
	return NULL;
}
