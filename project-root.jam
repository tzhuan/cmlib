variant release_static : release : <link>static ;
variant debug_static : debug : <link>static ;

project cmlib 
	:
	: default-build $(default-build)
	;

rule prefix ( dir : sequences * ) {
	local result ;
	for local x in $(sequences) {
		result += $(dir)/$(x) ;
	}
	return $(result) ;
}
